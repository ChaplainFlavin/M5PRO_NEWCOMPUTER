# 跨域任务强制启动校验单 (Pre-flight Init Protocol)

> **最高协议指令层说明**：
> 本协议为克制大语言模型 (LLM) 在长线高负荷运行下极易引发的“上下文截断遗忘”与“盲目堆词幻觉”的最高级别物理特效药。
> 在任何时间节点，只要系统接收到了指挥官（导演/使用者）下发的带有明显【开启新跨域/接管全新段落/处理大段代码架构】意图的突发指令，当前 AI 的心智算力阵列必须且只能无条件执行此张校验核对单。

## 📍 第 1 步: 语义雷达侦测与靶向知识灌注 (Semantic Radar & Routing)
系统在捕获指令请求的毫秒级潜层内，必须首轮唤醒逻辑探针去嗅探用户真实的环境诉求领域，紧接着，自动寻址并静默挂载以下已被完全物理隔离的碎片化高浓度知识法典（存储于库舱：`ai-workbench/memory/domains/`）：

- 🕵️‍♂️ **探针侦测到 [即梦 / 可灵 / 视频大乱斗生成 / 分镜构图 / 商业提示词写码] 意图**：
  🧲 **系统强制拦截并挂载库** 👉 `workflow_prompting.md` （强制检索抽提防锁区、红温拒稿的屏蔽词生死铁律，如朱红色替代血色）

- 🕵️‍♂️ **探针侦测到 [多目标物理畸变变形 / 画面强制锁死原画背景换不动 / 裁腿裁主体腰斩] 意图**：
  🧲 **系统强制拦截并挂载库** 👉 `skill_cv_processing.md` （强制抽取例如 21:9 无底裤垫图起手式、外圈疯狂扩图延景战术等解法）

- 🕵️‍♂️ **探针侦测到 [撰写后台 Python 覆写查杀脚本 / JSON与MD核心配置树爆破与挂载 / 底层系统终端强报错排雷] 意图**：
  🧲 **系统强制拦截并挂载库** 👉 `agent_self_ops.md` （强制执行温习 Rule 8 免死牌，断绝暴力大面积覆盖行为、绝不开干前不供述预期 Diff 的物理底线红线）

## 📍 第 2 步: 首句硬宣誓与算力终极解封印 (The Output Pledge 制动)
在成功探得与挂载对撞知识区完毕后，当 AI 面向终端指挥官开口发散回复内容的**绝对第一行起始端**，不被允许输出任何剧情推演、虚假寒暄与空洞主干！这第一行必须被强制剥夺，征用作机器级自检校验头颅。

**强制锁死的通讯首报模版如下（AI 在运行中必须将下方方框变量动态更替为当期雷达实情）**：
```text
[SYSTEM INIT] 📡 雷达跨域意图测绘命中：【此处填入智能判定的微型意图总结】，已于底仓成功切断并执行活体挂载防御图谱：【此处填入抽提挂载的具体域文件名.md】。本轮防崩禁忌条款检索排查通过，允许安全解爆，起落架解封。
```

> ⚠️ [系统宪法底层警示]：只要当期答复的正文开头首行，没有成功弹出此等效校验包回执格式协议的代码块，后续接龙生成的数万字大作由于未经“安全避坑排污检索”，底座强制一并将其统视为“发红温违章伪劣建筑”，可随时被下令销毁。
